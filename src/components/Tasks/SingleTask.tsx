import { taskTable } from '../../db/database.config';
import { ITask } from '../../db/types';
import { Draggable } from 'react-beautiful-dnd'

export default function SingleTask({...task}: ITask, {...provided}) {
	
	

  return (
	
  )
}
